"""3.2 Media Stream & Turn Loop — the media consumer package.

Package marker. The consumer itself lives in ``MediaStream.py`` and is re-exported
from ``apps.runtime.consumers`` (the parent ``__init__``), which is what
``routing.py`` imports.
"""
