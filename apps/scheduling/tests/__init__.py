"""Test package for `apps.scheduling` — the Calendar & Bookings module.

Sub-modules covered so far:

* `ContactDirectory` (4.1) — `test_models.py`, `test_services.py`,
  `test_forms.py`, `test_views.py`, `test_security.py`.
"""
